# ── Core web framework ────────────────────────────────────────────────────────
fastapi>=0.115.0             # 0.115+ ships py3.14 compatible deps
uvicorn[standard]>=0.30.1
python-multipart>=0.0.26     # required for FastAPI file uploads

# ── Settings / validation ─────────────────────────────────────────────────────
pydantic>=2.11.0             # 2.11+ has Python 3.14 pre-built wheels (no Rust compile needed)
pydantic-settings>=2.7.0    # compatible with pydantic 2.11+

# ── Azure OpenAI (LLM + Vision) ───────────────────────────────────────────────
openai>=1.30.1               # Azure OpenAI client

# ── PDF processing ────────────────────────────────────────────────────────────
PyMuPDF>=1.25.0              # fitz – fast embedded-text extraction + page-to-image (1.25+ has Python 3.14 wheels)

# ── Azure Storage (Blob + Table) ──────────────────────────────────────────────
azure-storage-blob>=12.20.0
azure-data-tables>=12.5.0

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=9.0.3
pytest-asyncio>=0.23.0
httpx>=0.27.0                # async test client for FastAPI
python-dotenv>=1.0.0
